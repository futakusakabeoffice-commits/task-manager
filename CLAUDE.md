# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code (claude.ai/code) 向けのガイドです。

## プロジェクト概要

**定食屋 ひなた食堂** の飲食店コーポレートサイト。静的 HTML/CSS で構築されたマルチページサイトで、ビルドツール・フレームワークは使用していません（プレーンな HTML/CSS＋モバイルナビ開閉用の素の JS 1ファイルのみ）。

※このサイトはサンプルとして作成した架空の店舗です。実在の店舗・団体とは関係ありません。住所・電話番号（`00-0000-0000` 等）はすべてダミー値です。

このサイトは Claude Design 経由で生成されたもので、`docs/DESIGN.md`（見た目の仕様）と `docs/CONTENTS.md`（テキスト・構成）の2ファイルが仕様のソース・オブ・トゥルースです。コードを変更する際は、実装がこの2ファイルと矛盾していないか必ず確認してください。

## リポジトリ構成

```
index.html       HOME（ヒーロー／ABOUT／本日のおすすめ定食／メディア掲載情報／店舗情報／お問い合わせ）
kodawari.html     こだわり（コンセプト／三つのこだわり／店主メッセージ）
menu.html         メニュー（定食一覧／単品・小鉢）
shop.html         店舗情報／アクセス
news.html         お知らせ一覧
privacy-policy.html  プライバシーポリシー（フッターの法務リンクからのみ遷移。グローバルナビには含まない）
styles.css        全ページ共通スタイル（CSSカスタムプロパティでトークン管理）
script.js         モバイルナビ（ハンバーガーメニュー）の開閉制御。全ページで <script defer> 読み込み
docs/DESIGN.md    デザインシステム仕様書（色・タイポグラフィ・余白・コンポーネント・レスポンシブ・a11y）
docs/CONTENTS.md  テキスト・構成の仕様書（各セクションの見出し・本文・メタ情報）
```

各 HTML はヘッダー（グローバルナビ）とフッター（サイトマップ・SNS・コピーライト）を共通で持ち、`<section id="...">` の粒度で `docs/CONTENTS.md` の見出しに対応しています。

## デザインシステム（要点。詳細は `docs/DESIGN.md` を参照）

- **トーン**: 和モダン、朱色×明朝体、和紙・古紙テクスチャ、老舗食堂らしい温かみと信頼感。
- **カラートークン**（`styles.css` の `:root` で CSS カスタムプロパティ化済み）:
  - `--color-primary: #E2592C`（朱色／CTA・タグ・見出しアクセント）
  - `--color-primary-dark: #C74A22`（ホバー用）
  - `--color-secondary: #D98C4A`
  - `--color-bg: #F1E4CE` / `--color-bg-light: #F8F0E1`
  - `--color-surface: #FDFBF6`
  - `--color-text: #231F1B` / `--color-text-body` / `--color-text-muted` / `--color-text-faint: #9C9187`
  - `--color-border: #E3D5BC`
- **フォント**: 見出しは `--font-mincho`（Shippori Mincho）、本文は `--font-serif`（Noto Serif JP）、英数装飾は `--font-garamond`（Cormorant Garamond）。ゴシック体は使わない。
- **新しい色・フォントをハードコードしない**。必ず既存の CSS カスタムプロパティを使う。
- ボタンはピル型（`border-radius: 24px`）が基本。カードは角丸 `12px`、影なし、フラット。
- レスポンシブはデスクトップファースト。ブレークポイントは `~767px`（モバイル）/ `768–1023px`（タブレット）/ `1024px~`（デスクトップ）。
- **モバイルヘッダー（≤767px、実装済み）**: `.site-header`（デスクトップの横並びナビ）を非表示にし、`.mobile-header` に切り替える。上段はロゴのみの帯、下段が2分割の固定バー（左: `Surface` 背景＋ハンバーガー＋「メニュー」／右: `Primary` 背景＋フォークアイコン＋「ご予約」）。ハンバーガーをタップすると `#mobile-nav`（オフキャンバスのリンク一覧）が開き、`script.js` が開閉・Escキー・背景クリック・リサイズ時のクローズを制御する。「ご予約」ボタンは `tel:` リンクに直接飛ばず、タップすると `#mobile-reserve-popover`（電話番号を表示するポップオーバー）が開き、その中の電話番号リンクをタップして初めて発信する2段階方式（ハンバーガーメニューと排他的に開閉、外側クリック・Escで閉じる）。新規ページを追加する場合は既存ページの `.mobile-header`（`.mobile-reserve-wrap` / `#mobile-reserve-popover` を含む）／`#mobile-nav` / `.mobile-nav-backdrop` のマークアップと `<script src="script.js" defer>` の読み込みをそのまま複製すること。
- アクセシビリティ: 本文コントラスト比 4.5:1 以上、画像には必ず `alt`、縦書きナビは視覚順と DOM 順を一致させる。詳細ルールは `docs/DESIGN.md` §7。

## コンテンツの扱い（詳細は `docs/CONTENTS.md` を参照）

- グローバルナビ: HOME / こだわり / メニュー / 店舗情報 / お知らせ / アクセス
- 文言・価格・店舗情報（住所・営業時間・定休日・席数など）を変更する場合は、まず `docs/CONTENTS.md` を更新してから HTML に反映する（HTML への直接の場当たり的な変更は避ける）。
- 画像は `.img-slot` によるプレースホルダー枠に `<img>` を重ねる形式（`images/` フォルダに規定のファイル名で置くと自動反映、無ければプレースホルダー表示のまま。詳細は `images/README.md`）。現状すべての枠に写真が差し込み済み（地図のみ `.img-slot` ではなく Google Maps の `<iframe>` 埋め込み）。alt テキストは `docs/CONTENTS.md` に記載の想定内容を踏襲している。撮影内容を差し替える際も同じ alt を踏襲する。
- 電話予約のみを想定（Web 予約フォームなし）。特定商取引法表記は省略（実店舗のみの飲食店のため）。プライバシーポリシーのみ最低限記載する方針。

## 既知の未実装項目

- 住所・電話番号（`00-0000-0000` 等）はダミーのまま。実店舗情報が決まり次第、`docs/CONTENTS.md` → 各 HTML の順に更新する。
- HOME・店舗情報ページの Google Maps 埋め込みは仮の地点（東京駅）。手順は `images/README.md`「アクセス地図について」を参照。
- メディア掲載セクション（HOME `#media`）のリンク先・食べログ URL 等はダミー。
- メニューページの「単品・小鉢」、お知らせページの記事一覧・ページネーション（2・3ページ目）はサンプル内容（`note` に明記済み）。実際の記事ページは未作成。
- `privacy-policy.html` の本文はサンプルの雛形（ページ内に注記あり）。実際の運用に合わせて内容を確認し、必要に応じて専門家に相談のうえ修正する。

## 開発時の注意

- ビルド・パッケージマネージャ・テストランナーは存在しない。ブラウザで直接 `index.html` 等を開くか、簡易な静的サーバー（例: `python3 -m http.server`）で確認する。
- 新規ページを追加する場合は、既存ページ（`index.html` 等）のヘッダー（`.site-header` と `.mobile-header`／`#mobile-nav` の両方）／フッター構造とナビゲーションのアクティブ状態（`class="is-active" aria-current="page"`）の付け方を踏襲する。
- `grid-template-columns` などレスポンシブに関わるプロパティを要素のインライン `style` で指定しない（同名クラスのメディアクエリより詳細度が高くなり、モバイル幅で崩れる）。差分が必要な場合は `.concept--home` のような修飾クラスを `styles.css` 側に定義すること。
- UI 変更を行う際は、変更後に対象ページをブラウザで実際に開いて確認すること（デスクトップ幅・タブレット幅・モバイル幅〈767px以下、320px程度の小型機も〉のすべてで、横スクロールが発生しないことも含めて確認する）。
